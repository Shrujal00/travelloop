/** Validates standard UUID strings (e.g. Supabase `gen_random_uuid()`). */
export function isUuidTripParam(value: string): boolean {
  return /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(
    value.trim()
  );
}
